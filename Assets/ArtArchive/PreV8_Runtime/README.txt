PRE-V8 ART BACKUP

This folder contains copies of the previous generated Goro atlas and pitch bird image.
The V8 match presentation does not use the old Goro atlas or the TeamKit recolour shader.

The original Resources/Duel/GoroKeeper.png remains in place so extracting this patch never
deletes an existing project asset. DemoCharacterArtV8 runs later and replaces it with the
approved individual orange/blue frames. It can be deleted manually after device testing.
